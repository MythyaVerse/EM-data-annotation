# CVAT Integration Requirements
# Install with: pip install -r requirements_cvat.txt

# Core CVAT SDK
cvat-sdk>=2.9.0

# Optional: CLI tool for CVAT
cvat-cli>=2.9.0

# Dependencies (usually installed automatically with cvat-sdk)
requests>=2.31.0
pillow>=10.0.0
attrs>=23.1.0

# For better JSON handling
orjson>=3.9.0

# Progress bars for uploads
tqdm>=4.65.0
